services:
  app:
    build: .
    image: datamatrix-detector:latest
    container_name: datamatrix-detector
    ports:
      - "8501:8501"
    volumes:
      - ./data:/data
    restart: unless-stopped
